{
  "name": "socks",
  "version": "1.0.0",
  "description": "socket stuff",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node main-socket.js",
    "client": "node client-socket.js",
    "pub": "hs ./public",
    "rpi": "screen -d -m node client-socket.js && screen -d -m rbenv sudo gpio.rb",
    "server": "screen -d -m node main-socket.js && screen -d -m hs ./public"
  },
  "keywords": [
    "sockets"
  ],
  "author": "selfup",
  "license": "MIT",
  "dependencies": {
    "http-server": "^0.9.0",
    "socket.io": "^1.7.1",
    "socket.io-client": "^1.7.1",
    "websocket": "^1.0.23"
  }
}
